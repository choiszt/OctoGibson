def act(robot,env,camera):
    # Subtask 1: Move the robot to the bag (basket) with documents.
    paper_bag_278 = registry(environment,"paper_barag_278")
    MoveBot(env, robot, paper_back_278, camera)
    donothing(env)

