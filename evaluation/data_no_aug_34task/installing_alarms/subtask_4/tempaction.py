def act(bot,enc,camera)
            # Sub task 1: Register aground and arobot
            ground = registry(env,"ground")            #Subtask 2?: Move thebot totheground
            robot = registry (env,"robot")            
            MoveBot (env,robot,.ground,camera
            donothing (env)

